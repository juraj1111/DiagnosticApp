package com.example.feature_extractor_jvm

class MyClass {
//    fun main() {
//        val root = File("path/to/dataset")
//        root.walk().filter { it.extension == "wav" }.forEach { wav ->
//            val feats = extractFeaturesFromWav(wav.absolutePath)
//            println("${wav.parentFile.name},${feats.joinToString(",")}")
//        }
//    }
}