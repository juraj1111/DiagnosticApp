<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiagnosticApp"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DiagnosticApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.PatientInfoActivity" />
        <activity android:name=".ui.PatientActivity" />
        <activity android:name=".ui.VoiceTasksListActivity"/>
        <activity android:name=".ui.VoiceTaskActivity"/>
        <activity android:name=".ui.WritingTasksListActivity"/>
        <activity android:name=".ui.WritingTestActivity"/>
        <activity android:name=".ui.WritingTaskActivity"/>
        <activity android:name=".ui.WritingTaskExecutionActivity"/>

    </application>
</manifest>
